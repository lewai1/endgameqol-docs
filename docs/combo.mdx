---
title: Combo Meter
description: Chain kills to climb damage tiers with speed, heal, crit, and lifesteal bonuses.
authors:
    - name: "Lewai"
---

# Combo Meter

Chain kills together to climb damage tiers and unlock powerful combat bonuses.

## Overview

The Combo Meter rewards aggressive, continuous combat. Every **kill** increases your combo counter. As the counter climbs, you pass through four damage tiers, each granting increasingly powerful effects. Stop killing for too long, and the meter decays back to zero.

## Damage Tiers

| Tier | Multiplier | Bonus Effect |
|:-----|:----------:|:-------------|
| **x2** | 110% | — |
| **x3** | 125% | Speed boost |
| **x4** | 150% | Heal on kill + critical chance increase |
| **FRENZY** | 200% | Lifesteal on every kill |

## Tier Effects

- **x2** — Damage multiplier only. The baseline reward for keeping your combo alive.
- **x3 — Speed** — A noticeable movement speed boost that helps you close gaps and maintain pressure.
- **x4 — Heal + Crit** — Each kill restores a small amount of health. Your critical strike chance increases, making every swing count.
- **FRENZY — Lifesteal** — Maximum damage output with lifesteal on every kill. Sustain through aggression. Reaching Frenzy is the pinnacle of the combo system.

## HUD Overlay

When the combo meter is active, a HUD overlay appears on screen showing:

- **Combo Count** — The current number of consecutive kills
- **Current Tier** — Color-coded indicator showing your active tier
- **Decay Timer** — A shrinking bar showing how long until the combo drops
- **Personal Best** — Your highest combo count ever, saved persistently across sessions

## Decay & Reset

The combo meter begins to decay after **5 seconds** without landing a kill (configurable). The decay timer resets with every successful kill. If the timer reaches zero, the combo resets entirely and all tier bonuses are lost.

Taking fatal damage also resets the combo to zero, regardless of the remaining decay timer.

## Configuration

All values are in the `Combo` section of `EndgameConfig.json`, or adjust via `/egconfig` Combat tab.

| Key | Default | Description |
|:----|:--------|:------------|
| `Enabled` | true | Enable or disable the combo meter system |
| `TimerSeconds` | 5.0 | Seconds of inactivity before the combo meter resets (1.0 -- 30.0) |
| `DamageX2` | 1.10 | Damage multiplier at x2 tier (110%) |
| `DamageX3` | 1.25 | Damage multiplier at x3 tier (125%) |
| `DamageX4` | 1.50 | Damage multiplier at x4 tier (150%) |
| `DamageFrenzy` | 2.00 | Damage multiplier at FRENZY tier (200%) |
| `TierEffectsEnabled` | true | Enable tier bonus effects (speed, heal, lifesteal) |
| `DecayEnabled` | true | Enable combo decay over time |
